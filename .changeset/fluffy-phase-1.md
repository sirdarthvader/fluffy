---
"@fluffy/core": patch
"@fluffy/cli": patch
"create-fluffy-app": patch
---

Add the first usable Fluffy dev workflow with file-based routing, server rendering,
browser hydration, the `fluffy dev` CLI, an example app, and repository formatting
and linting checks.
