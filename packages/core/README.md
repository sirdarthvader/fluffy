# @fluffee/core

Core framework runtime for Fluffy.

Phase 1 owns:

- file-based route discovery from `src/pages`
- Vite-powered development middleware
- server rendering for matched page routes
- a generated browser hydration entry

Static export and production build orchestration are intentionally out of scope until later roadmap phases.
