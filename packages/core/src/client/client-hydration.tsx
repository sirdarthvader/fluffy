/**
 * Reserved home for future browser hydration helpers.
 *
 * The current Phase 1 implementation keeps hydration code in the generated
 * Vite virtual module so the example app can work without requiring a fixed
 * client entry file.
 */
