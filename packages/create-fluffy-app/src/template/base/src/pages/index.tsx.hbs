import { FC } from 'react';

const HomePage: FC = () => {
  return (
    <div className="{{#if useTailwind}}p-4 bg-gray-100{{/if}}">
      <h1>Welcome to {{projectName}}!</h1>
      {{#if useTS}}
      <p>TypeScript is enabled!! 🎉</p>
      {{/if}}
    </div>
  );
};

export default HomePage;